# CSC Allas Configuration (Swift/OpenStack Protocol)
# Copy this file to .env and fill in your actual credentials

# OpenStack Authentication URL (default for CSC Allas)
OS_AUTH_URL=https://pouta.csc.fi:5001/v3

# Your CSC username
OS_USERNAME=your_csc_username_here

# Your CSC password (or use application credentials - see README)
OS_PASSWORD=your_csc_password_here

# Your CSC project name (format: project_XXXXXXX)
# Find this in CSC My CSC portal
OS_PROJECT_NAME=project_XXXXXXX

# Domain settings (usually Default for CSC)
OS_PROJECT_DOMAIN_NAME=Default
OS_USER_DOMAIN_NAME=Default

# Name of your Allas container/bucket
# Create this in CSC Allas web interface
DATA_BUCKET=your_container_name_here

# Pipeline Configuration
PIPELINE_SCHEDULE_INGESTION=15  # minutes between ingestion runs
PIPELINE_SCHEDULE_CLEANING=10   # minutes between cleaning runs

# Data Quality Thresholds (adjust based on your data)
QUALITY_COMPLETENESS_THRESHOLD=80   # % of non-null values required
QUALITY_UNIQUENESS_THRESHOLD=95     # % of unique records required
QUALITY_CONSISTENCY_THRESHOLD=90    # % of consistent data formats required
QUALITY_VALIDITY_THRESHOLD=85       # % of valid values required

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ==========================================
# FOR STUDENTS: How to get these values
# ==========================================

# 1. OS_USERNAME & OS_PASSWORD:
#    - Your normal CSC account credentials
#    - Same as what you use to login to my.csc.fi

# 2. OS_PROJECT_NAME:
#    - Login to https://my.csc.fi
#    - Go to "Projects" section
#    - Look for format: project_2015319 (your number will be different)

# 3. DATA_BUCKET:
#    - Login to CSC Allas web interface
#    - Create a new container/bucket
#    - Use that name here
#    - Upload some CSV/JSON files for testing

# 4. For production (recommended):
#    - Use Application Credentials instead of password
#    - Go to https://pouta.csc.fi
#    - Navigate to Identity > Application Credentials
#    - Create new credential
#    - Replace OS_USERNAME/OS_PASSWORD with:
#      OS_APPLICATION_CREDENTIAL_ID=your_app_cred_id
#      OS_APPLICATION_CREDENTIAL_SECRET=your_app_cred_secret

# ==========================================
# SECURITY WARNING FOR STUDENTS
# ==========================================
# 
# NEVER commit this file with real credentials to Git!
# The .gitignore file should exclude .env files
# 
# For GitHub Actions deployment, add these as repository secrets:
# GitHub repo > Settings > Secrets and variables > Actions

# Data Retention
DATA_RETENTION_DAYS=30
MAX_FILE_SIZE_MB=100

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
